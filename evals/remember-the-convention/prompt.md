---
description: >-
    The other half of learn: a project convention rather than an error-to-fix pair.
    Worth its own case because the description leads with the error-to-fix framing,
    so a convention may route worse than a bug fix does.
expected_outcome: >-
    /handbook:learn. If this case scores below capture-this-fix, the description
    under-serves the convention half of its own promise.
tags: [nl]
max_turns: 6
timeout_seconds: 180
allowed_tools: [Read, Glob, Grep, Skill]
---

we always run the migration before the seed here, make sure that sticks
