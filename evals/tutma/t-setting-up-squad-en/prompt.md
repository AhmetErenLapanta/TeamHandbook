---
description: >-
    RETIRED, and kept here as the record rather than deleted. This sentence repeats two
    entries of init's own trigger list, clause for clause: a model matching on triggers
    scores it whether routing is healthy or not, so it cannot fail when routing regresses
    and it was inflating the held-out number. The leak check found it as soon as that check
    was fixed to actually read the sealed sentences. The tag below is deliberately one no
    package selects, so the case no longer runs.
expected_outcome: >-
    Not scored. It used to expect /handbook:init.
tags: [atildi-k26]
max_turns: 8
timeout_seconds: 300
allowed_tools: [Read, Glob, Grep, Skill]
---

I'm the one setting this up for the squad, we need one shared place for how we work
