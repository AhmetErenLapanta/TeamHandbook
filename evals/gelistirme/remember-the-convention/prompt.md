---
description: >-
    The one case measured to hold a real product defect rather than a defect of this
    package: with full context it still lost, because the model reached for Claude
    Code's own memory instead. The prior turns supply both the rule and the fact that
    the team shares it, so "here" and "we" refer to something.
expected_outcome: >-
    /handbook:learn. A convention the whole team follows is the team-facing case for
    learn. Saving it to memory or a CLAUDE.md is the competing answer this case is
    meant to detect, and it is a miss.
tags: [nl-gelistirme]
max_turns: 6
timeout_seconds: 180
allowed_tools: [Read, Glob, Grep, Skill]
---

we always run the migration before the seed here, make sure that sticks
